import Router from "../../structures/Router";

const router = new Router();

router.post((req, res) => {
  // create a node by it's ip, port, label, authorization & then add the url to a script that will check if it is up or not (websocket)
});

export default router;
